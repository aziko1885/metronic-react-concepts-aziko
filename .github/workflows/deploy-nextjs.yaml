name: Deploy Next.js Starter Kit

on:
  push:
    branches: [nonexistent-branch]
  workflow_dispatch:

env:
  NODE_VERSION: 20.x
  DEPLOY_PORT: 3017
  APP_NAME: metronic-html-concepts
  APP_URL: https://keenthemes.com/metronic/concepts/nextjs/

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            cd /var/www/metronic/metronic-html-concepts/typescript/nextjs

            # Pull latest changes
            git reset --hard && git pull

            # Install dependencies
            npm ci -f || npm i -f

            # Debug: Show contents of existing .env.production file
            echo "=== Current .env.production Contents ==="
            if [ -f .env.production ]; then
              cat .env.production
            else
              echo ".env.production file does not exist"
            fi
            echo "========================================"

            # Build application on server
            npm run build

            # Restart application
            export PORT=${{ env.DEPLOY_PORT }}
            pm2 restart "${{ env.APP_NAME }}" --update-env || pm2 start npm --name "${{ env.APP_NAME }}" -- start

      - name: Deployment status
        run: |
          echo "✅ Deployment completed successfully"
          echo "🌐 Environment is available at: ${{ env.APP_URL }}"
